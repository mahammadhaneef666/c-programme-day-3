#include <stdio.h>
#include <string.h>
int main()
{
    char str[100];
     printf("Enter any word : ");
    scanf("%s",str);
 int len,i;
 len = strlen(str);
    printf("the Reverse of the word :");
    for(i = len-1; i>=0; i--)
    {
    scanf("%s",str[i]);}
 return 0;
}
